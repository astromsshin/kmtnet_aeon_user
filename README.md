The examples accessing the KMTNet AEON API are presented for cases 
using curl and httpie. We plan to include Python examples in this repository.

Generally, you start by creating the API token. See the examples of create_token. 
You can submit observation requests with the created token.
